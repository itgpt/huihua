version: '3.8'

services:
  ai-drawing-tool:
    build: .
    container_name: ai-drawing-tool
    ports:
      - "8000:80"
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html/:ro
